name: Build and Testname: Build and Test



on:on:

  push:  push:

    branches: [main, develop]    branches: [main, develop]

  pull_request:  pull_request:

    branches: [main, develop]    branches: [main, develop]



jobs:jobs:

  build:  build:

    name: Build and Test    runs-on: ubuntu-latest

    runs-on: ubuntu-latest

    steps:

    steps:      - name: Checkout code

      - name: Checkout code        uses: actions/checkout@v4

        uses: actions/checkout@v4        with:

          fetch-depth: 0 # Shallow clones should be disabled for SonarCloud

      - name: Set up JDK 21

        uses: actions/setup-java@v4      - name: Set up JDK 21

        with:        uses: actions/setup-java@v4

          java-version: "21"        with:

          distribution: "temurin"          java-version: "21"

          cache: "maven"          distribution: "temurin"

          cache: "maven"

      - name: Build with Maven

        run: ./mvnw clean install -DskipTests      - name: Cache Maven packages

        uses: actions/cache@v3

      - name: Run tests        with:

        run: ./mvnw test          path: ~/.m2

          key: ${{ runner.os }}-m2-${{ hashFiles('**/pom.xml') }}

      - name: Generate JaCoCo coverage report          restore-keys: ${{ runner.os }}-m2

        run: ./mvnw jacoco:report

      - name: Build with Maven

      - name: Archive coverage reports        run: ./mvnw clean install -DskipTests

        if: always()

        uses: actions/upload-artifact@v3      - name: Run tests

        with:        run: ./mvnw test

          name: coverage-report

          path: target/site/jacoco/      - name: Generate JaCoCo coverage report

          retention-days: 5        run: ./mvnw jacoco:report


      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v3
        with:
          files: ./target/site/jacoco/jacoco.xml
          flags: unittests
          name: codecov-umbrella

      - name: Archive build artifacts
        uses: actions/upload-artifact@v3
        with:
          name: petclinic-jar
          path: target/*.jar
          retention-days: 5

      - name: Archive test results
        if: always()
        uses: actions/upload-artifact@v3
        with:
          name: test-results
          path: target/surefire-reports/
          retention-days: 5

      - name: Archive coverage reports
        uses: actions/upload-artifact@v3
        with:
          name: coverage-report
          path: target/site/jacoco/
          retention-days: 5
